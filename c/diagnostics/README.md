# C++ Engineering Diagnostics

These programs exercise experimental or hardware-verification workflows and
are not application templates. Programs that move the hand require an explicit
runtime opt-in.

```bash
./c/build/diagnostics/collision_detection --run
```
